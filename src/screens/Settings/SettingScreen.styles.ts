import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      },
      content: {
        flex: 1,
        width: '100%',
      },
      footer: {
        backgroundColor: '#ccc',
        padding: 10,
        alignItems: 'center',
        width: '100%',
      },
});